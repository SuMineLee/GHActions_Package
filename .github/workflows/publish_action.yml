name: package
on: 
 release:
  type: [created]
  
jobs:
 build:
  runs-on: ubuntu-latest
  steps:
   - name: checkout github repo
     uses: actions/checkout@v2
   - name: setup node
     uses: actions/setup-node@v1
     with:
      node-verison: 12
   - run: npm ci
   - run: npm test
